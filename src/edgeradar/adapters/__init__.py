"""Source adapters.

Each supported platform implements `SourceAdapter` (see base.py). Adding a new
data source = one new module here implementing one class. Phase 1 adds the first
two concrete adapters (Kalshi + Manifold).
"""
